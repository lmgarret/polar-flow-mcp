bin/
/polar-flow-mcp
.env
*.db
.mcp.json
/site/

# Persisted session secrets — never commit
polar-cookies.json
*-cookies.json

# SQLite sidecar files (*.db already covered)
*.db-shm
*.db-wal

# GSD internal planning artifacts
.planning/
