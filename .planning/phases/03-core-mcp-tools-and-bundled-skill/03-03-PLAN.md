---
phase: 03-core-mcp-tools-and-bundled-skill
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - skill/polar-coach/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03, SKILL-04]
must_haves:
  truths:
    - "skill/polar-coach/SKILL.md exists and contains a trigger description Claude can use to decide when to invoke the polar-flow-mcp tools"
    - "All 4 registered tool names appear verbatim in SKILL.md: get_user_info, create_training_target, list_training_targets, delete_training_target"
    - "HR zone table maps all 5 labels (easy, aerobic, tempo, threshold, vo2max) to zones (Z1..Z5) with coaching descriptions"
    - "Worked example shows a 5x1km threshold session as a full create_training_target call with the documented phases array shape"
    - "Worked example covers iterative marathon-plan construction (ask user, then create per-week)"
    - "Safe degradation guidance instructs Claude to check tool availability before calling and to direct users to deployment docs if the server is unreachable"
    - "Both installation paths are documented: Claude Desktop/Code skills directory AND Claude.ai project file upload"
    - "Warmup/cooldown defaults (10 min warmup, 5 min cooldown) and Polar 18:00 default scheduled time are stated"
  artifacts:
    - path: "skill/polar-coach/SKILL.md"
      provides: "Polar coach skill — trigger, tools, zones, examples, degradation, install paths"
      min_lines: 150
      contains: "create_training_target"
  key_links:
    - from: "skill/polar-coach/SKILL.md"
      to: "internal/mcp/mcp.go tool name registrations"
      via: "Tool names quoted verbatim (no drift between SKILL.md and mcp.NewTool first args)"
      pattern: "get_user_info.*create_training_target.*list_training_targets.*delete_training_target"
---

<objective>
Author the bundled `skill/polar-coach/SKILL.md` that teaches Claude how to use the four polar-flow-mcp tools without any user configuration. Covers trigger conditions, exact tool names, HR zone vocabulary, worked examples (5x1km threshold session, marathon-plan iterative approach), when NOT to call tools, safe degradation, and both installation paths.

Purpose: Delivers SKILL-01 (trigger + 4 tool names + HR zone table + defaults), SKILL-02 (worked examples), SKILL-03 (when-not-to-call + safe degradation), SKILL-04 (both installation paths).

Output: Single markdown file at `skill/polar-coach/SKILL.md`. No code changes, no compilation impact.

Depends on Plans 01 and 02 only for tool name verification — names must match the registered tool names exactly. Place this in Wave 3 so the tool-name authority (`internal/mcp/mcp.go`) is stable when SKILL.md is written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-core-mcp-tools-and-bundled-skill/03-CONTEXT.md
@.planning/phases/03-core-mcp-tools-and-bundled-skill/03-RESEARCH.md
@.planning/phases/03-core-mcp-tools-and-bundled-skill/03-PATTERNS.md
@.planning/phases/03-core-mcp-tools-and-bundled-skill/03-01-PLAN.md
@.planning/phases/03-core-mcp-tools-and-bundled-skill/03-02-PLAN.md
@internal/mcp/mcp.go

<interfaces>
Tool names to reference verbatim (from `mcp.NewTool("name", ...)` first arg — verify by grepping `internal/mcp/mcp.go` before writing SKILL.md):

- `get_user_info` — Phase 2; returns linked Polar account info
- `create_training_target` — Phase 3 Plan 01
- `list_training_targets` — Phase 3 Plan 02
- `delete_training_target` — Phase 3 Plan 02

create_training_target argument shape (from Plan 01 Task 3):
```
{
  "name": string (required, max 45 chars),
  "date": string ISO 8601 YYYY-MM-DD (required),
  "time": string HH:MM (optional, default "18:00"),
  "sport": string (optional, default "RUNNING"),
  "phases": [                          // required, at least 1, max 50
    { "type": "warmup", "duration_s": int },
    { "type": "repeat", "reps": int, "goal": {"distance_m": number} OR {"duration_s": int},
      "intensity": {"label": "easy|aerobic|tempo|threshold|vo2max"} OR {"hr_zone": int 1-5},
      "recovery": {"duration_s": int}  // optional
    },
    { "type": "cooldown", "duration_s": int }
  ]
}
```

list_training_targets argument shape:
```
{ "from_date": string YYYY-MM-DD (optional, default today UTC),
  "to_date":   string YYYY-MM-DD (optional, default today+30 UTC) }
```

delete_training_target argument shape:
```
{ "target_id": string (required) }
```

HR zone mapping (D-06, locked):
```
easy      -> Z1   (recovery, conversational)
aerobic   -> Z2   (easy aerobic, comfortable)
tempo     -> Z3   (steady, comfortably hard)
threshold -> Z4   (hard, sustained — lactate threshold)
vo2max    -> Z5   (very hard, short intervals)
```

Defaults to document (D-04, D-09, plus Polar):
- Warmup: 10 minutes (600s)
- Cooldown: 5 minutes (300s)
- Scheduled time: 18:00 (Polar default; tool default)
- list date range: today through +30 days

Installation paths (D-13):
1. Claude Desktop / Claude Code: copy `skill/polar-coach/` to your skills directory. The skill is auto-discovered if SKILL.md is present at that path.
2. Claude.ai web: upload SKILL.md (and its directory if multi-file) as a project file.

Safe degradation (D-12):
- Before calling any polar-flow-mcp tool, Claude should confirm the server is connected (the tool appears in the MCP tool list).
- If unavailable: tell the user the server is not connected, link to deployment docs at the project repository's `docs/deployment/` (or `README.md` quickstart for v1 — Phase 4 produces full deployment docs).
- Do NOT fabricate tool calls or simulate results when the server is unreachable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skill/polar-coach/SKILL.md with all required sections</name>
  <files>skill/polar-coach/SKILL.md</files>
  <read_first>
    - internal/mcp/mcp.go (verify exact tool name strings — `grep -E 'mcpgo.NewTool\("[^"]+"' internal/mcp/mcp.go` before writing names into SKILL.md)
    - .planning/phases/03-core-mcp-tools-and-bundled-skill/03-CONTEXT.md (D-06, D-09, D-11, D-12, D-13 — all locked decisions for SKILL content)
    - .planning/phases/03-core-mcp-tools-and-bundled-skill/03-01-PLAN.md (phases array shape, intensity label list, default values used by handler)
    - .planning/phases/03-core-mcp-tools-and-bundled-skill/03-02-PLAN.md (list and delete arg shapes, 404 behavior wording)
  </read_first>
  <action>

**Pre-write verification step**: run `grep -E 'mcpgo.NewTool\("[^"]+"' internal/mcp/mcp.go` and confirm the 4 names listed are exactly: `get_user_info`, `create_training_target`, `list_training_targets`, `delete_training_target`. If any name differs, use the actual registered name in SKILL.md — registration is authoritative.

**Create `skill/polar-coach/SKILL.md`** with the following structure and content. Sections must appear in this order (per D-11). Markdown headings use `##` for top-level sections so the file parses cleanly when uploaded to Claude.ai or auto-loaded by Claude Desktop.

```markdown
# polar-coach

A Claude skill for managing Polar Flow training targets via the polar-flow-mcp server.
With this skill installed, you can ask Claude in plain language to create, list, or
delete workouts in Polar Flow — no UI navigation required.

## When to use this skill

Activate this skill when the user's message involves any of:

- Polar Flow, Polar watch, or Polar account
- creating, scheduling, planning, or modifying a training session, workout, run, or interval session
- terms like "intervals", "tempo run", "long run", "threshold", "vo2max", "easy run"
- training plans (5k, 10k, half marathon, marathon, base building, taper)
- listing upcoming planned workouts or removing one that was created in error

If the message is about *analyzing past sessions, viewing performance data, or syncing devices*,
this skill does NOT apply — see the "When NOT to call tools" section below.

## Tools

Four tools are exposed by the polar-flow-mcp server. Use the **exact** names below.

| Tool | Purpose |
|------|---------|
| `get_user_info` | Confirm the user's Polar account is linked. Call this FIRST if you're unsure whether the user has linked their account. Returns the Polar user ID or a hint to visit `/oauth/login`. |
| `create_training_target` | Create a single scheduled workout in Polar Flow. Accepts a flat phases array (warmup, one or more repeat blocks, cooldown). |
| `list_training_targets` | List upcoming training targets in a date range. Defaults to today through +30 days. |
| `delete_training_target` | Delete a training target by its `target_id` (obtained from `list_training_targets` or the response of `create_training_target`). |

### Tool availability check

Before calling any of these tools, confirm the polar-flow-mcp server is connected (the
tool should appear in your available MCP tool list). If it does not:

1. Tell the user the polar-flow-mcp server is not connected.
2. Point them at the project README and deployment docs (`docs/deployment/`) for setup.
3. Do NOT fabricate tool calls or simulate results.

## HR zones and intensity vocabulary

Polar Flow uses heart-rate zones Z1-Z5. The `intensity` field on a repeat phase accepts
either a `label` (preferred — coaching vocabulary) or an `hr_zone` integer (escape hatch).

| Label | Zone | Coaching meaning |
|-------|------|------------------|
| `easy` | Z1 | Recovery pace. Conversational, fully aerobic. Use for shakeout runs, warm-ups, recovery jogs. |
| `aerobic` | Z2 | Easy aerobic / base. Comfortable, sustainable for long runs. Bulk of weekly volume. |
| `tempo` | Z3 | Steady-state, comfortably hard. Marathon to half-marathon effort. |
| `threshold` | Z4 | Lactate threshold. Hard, sustainable for ~30-60 min. Classic 1km repeats, cruise intervals. |
| `vo2max` | Z5 | Very hard. Short repeats (1-5 min) at near-maximal aerobic effort. |

Always prefer labels over zone numbers in conversation with the user, then pass the label
through to `create_training_target`. Use `hr_zone` numerically only if the user explicitly
gives one ("zone 4", "Z3").

## Defaults

When the user does not specify, use these defaults — they match what Polar Flow expects
and what the polar-flow-mcp server applies:

- **Warmup**: 10 minutes (`duration_s: 600`). Always include unless the user says "no warmup".
- **Cooldown**: 5 minutes (`duration_s: 300`). Always include unless the user says "no cooldown".
- **Scheduled time**: 18:00. Pass `time: "18:00"` or omit (handler defaults to 18:00).
- **Sport**: RUNNING. Omit unless the user names a different sport.

## Worked examples

### Example 1: 5x1km threshold session

User says: *"create a 5x1km threshold session with 2 minute recovery for next Thursday"*.

You should:

1. Resolve "next Thursday" to an ISO date based on the user's reference date.
2. Use the default warmup (10 min) and cooldown (5 min) since the user did not specify.
3. Map "threshold" -> `label: "threshold"` (handler maps to Z4 internally).
4. Call `create_training_target` with:

```json
{
  "name": "5x1km Threshold",
  "date": "2026-05-21",
  "time": "18:00",
  "sport": "RUNNING",
  "phases": [
    { "type": "warmup", "duration_s": 600 },
    {
      "type": "repeat",
      "reps": 5,
      "goal": { "distance_m": 1000 },
      "intensity": { "label": "threshold" },
      "recovery": { "duration_s": 120 }
    },
    { "type": "cooldown", "duration_s": 300 }
  ]
}
```

The response includes the new target's `ID`. Tell the user the session was created and
include the ID (they can use it to delete the session if it was wrong).

### Example 2: Iterative marathon plan

User says: *"build me a 12-week marathon plan starting in three weeks"*.

A marathon plan is many sessions — do NOT create all 36+ sessions silently. Ask the user
first, then create per-week (or per-block) so they can review and steer.

1. Ask first: peak weekly mileage? long-run day? quality-session day(s)? rest day(s)?
2. Once you have a structure, propose week 1 in plain prose, ask for confirmation.
3. On confirmation, call `create_training_target` once per session in week 1.
4. Repeat for each subsequent block (e.g., 4-week phases).
5. After each block, summarize what was created and confirm before continuing.

This iterative approach keeps the user in control and avoids polluting their Polar Flow
calendar with sessions they did not explicitly approve.

### Example 3: Mixed-zone workout (two distinct repeat blocks)

The tool supports multiple repeat blocks in a single workout. Example for
"3x1km at threshold, then 2x500m at vo2max":

```json
{
  "name": "Threshold + VO2max ladder",
  "date": "2026-05-22",
  "phases": [
    { "type": "warmup", "duration_s": 600 },
    {
      "type": "repeat", "reps": 3,
      "goal": { "distance_m": 1000 },
      "intensity": { "label": "threshold" },
      "recovery": { "duration_s": 90 }
    },
    {
      "type": "repeat", "reps": 2,
      "goal": { "distance_m": 500 },
      "intensity": { "label": "vo2max" },
      "recovery": { "duration_s": 180 }
    },
    { "type": "cooldown", "duration_s": 300 }
  ]
}
```

### Example 4: Listing and deleting

User says: *"what do I have planned this week?"*

Call `list_training_targets` with `from_date` set to today and `to_date` set to the end
of the current week (or omit both to use the default today..+30 days). Format the result
for the user.

User says: *"delete the one on Friday — I'm not going to do it"*.

1. Use `list_training_targets` to find the matching session and its `target_id`.
2. Confirm with the user which session they mean (show name + date).
3. Call `delete_training_target` with that `target_id`.
4. If the response says "not found", the session was already deleted — tell the user
   plainly; this is not a failure.

## When NOT to call tools

Do NOT call polar-flow-mcp tools when the user is asking about:

- **Past sessions** — analytics, splits, heart-rate review, sync status. The polar-flow-mcp
  server writes training targets only; it does not read historical activity data.
- **Pace targets in min/km or min/mile** — v1 supports HR zones only. If the user asks for
  pace-based intensity, explain the limitation and offer the closest HR zone equivalent.
- **Power targets in watts** — v1 does not support power. Same limitation; offer HR zones.
- **Polar account creation, sync, or device pairing** — outside this skill's scope. Refer
  to the official Polar Flow app.
- **General training advice that does not need to create a workout** — answer normally
  without invoking tools.

## Safe degradation

The polar-flow-mcp server is self-hosted and may not always be reachable.

- If the four tools above are not in your available MCP tool list, the server is not
  connected. Tell the user this clearly and link them to the deployment docs in the
  project repository (start with `README.md`; `docs/deployment/` once Phase 4 ships).
- If a tool call returns an error mentioning "No Polar account linked" or
  "Visit /oauth/login", the user has not completed the Polar OAuth link. Tell them
  to visit `/oauth/login` on their polar-flow-mcp deployment (behind their reverse-proxy
  auth) and authorize Polar access before retrying.
- If a tool call returns an error containing "status 4xx" or "status 5xx", surface the
  message to the user verbatim and ask them to check their server logs. Do not retry
  silently — the Polar API may be rejecting a malformed request that needs a fix on the
  server side.

## Installation

There are two ways to install this skill:

### Option 1: Claude Desktop or Claude Code (local skills directory)

1. Locate your Claude skills directory. On Claude Code this is typically
   `~/.claude/skills/`; on Claude Desktop see the official skills documentation for
   your platform's path.
2. Copy the `polar-coach/` directory (containing this `SKILL.md`) into that directory.
3. Restart Claude. The skill is auto-discovered.

### Option 2: Claude.ai (project file upload)

1. Open your Claude.ai project (or create one).
2. Upload this `SKILL.md` file as a project file.
3. The skill becomes active for all chats in that project.

Both paths give Claude the same vocabulary and tool-calling guidance.
```

After writing, verify:
1. File path is exactly `skill/polar-coach/SKILL.md` (create parent directories if needed).
2. All 4 tool names appear verbatim (grep gates below).
3. All 5 intensity labels appear in the HR zone table (grep gates below).
4. Both installation paths are documented.
5. File length >= 150 lines (substantive coverage, not a stub).
  </action>
  <verify>
    <automated>test -f skill/polar-coach/SKILL.md &amp;&amp; [ "$(wc -l &lt; skill/polar-coach/SKILL.md)" -ge 150 ] &amp;&amp; for tool in get_user_info create_training_target list_training_targets delete_training_target; do grep -q "$tool" skill/polar-coach/SKILL.md || { echo "MISSING: $tool"; exit 1; }; done &amp;&amp; for label in easy aerobic tempo threshold vo2max; do grep -qi "$label" skill/polar-coach/SKILL.md || { echo "MISSING label: $label"; exit 1; }; done &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f skill/polar-coach/SKILL.md` exits 0
    - `wc -l < skill/polar-coach/SKILL.md` is >= 150
    - `grep -c "get_user_info" skill/polar-coach/SKILL.md` returns >= 1
    - `grep -c "create_training_target" skill/polar-coach/SKILL.md` returns >= 2
    - `grep -c "list_training_targets" skill/polar-coach/SKILL.md` returns >= 1
    - `grep -c "delete_training_target" skill/polar-coach/SKILL.md` returns >= 1
    - `grep -c -E "Z1|Z2|Z3|Z4|Z5" skill/polar-coach/SKILL.md` returns >= 5
    - `grep -ci "easy" skill/polar-coach/SKILL.md` returns >= 1
    - `grep -ci "aerobic" skill/polar-coach/SKILL.md` returns >= 1
    - `grep -ci "tempo" skill/polar-coach/SKILL.md` returns >= 1
    - `grep -ci "threshold" skill/polar-coach/SKILL.md` returns >= 2
    - `grep -ci "vo2max" skill/polar-coach/SKILL.md` returns >= 1
    - `grep -c "10 min" skill/polar-coach/SKILL.md` returns >= 1  # warmup default
    - `grep -c "5 min" skill/polar-coach/SKILL.md` returns >= 1  # cooldown default
    - `grep -c "18:00" skill/polar-coach/SKILL.md` returns >= 1  # scheduled time default
    - `grep -ci "claude desktop\|claude code" skill/polar-coach/SKILL.md` returns >= 1  # install path 1
    - `grep -ci "claude.ai" skill/polar-coach/SKILL.md` returns >= 1  # install path 2
    - `grep -ci "when not to call\|do not call\|do NOT call" skill/polar-coach/SKILL.md` returns >= 1
    - `grep -c "/oauth/login" skill/polar-coach/SKILL.md` returns >= 1  # safe degradation hint
    - `grep -ci "5x1km\|5×1km" skill/polar-coach/SKILL.md` returns >= 1  # worked example 1
    - `grep -ci "marathon" skill/polar-coach/SKILL.md` returns >= 1  # worked example 2
    - Tool name parity check: for each tool registered in internal/mcp/mcp.go, the same name appears in SKILL.md. Manual cross-check via `for n in $(grep -oE 'mcpgo.NewTool\("[^"]+"' internal/mcp/mcp.go | sed 's/.*"\(.*\)"$/\1/'); do grep -q "$n" skill/polar-coach/SKILL.md || echo "DRIFT: $n"; done` produces empty output (no DRIFT lines)
  </acceptance_criteria>
  <done>skill/polar-coach/SKILL.md exists with all required sections per D-11, all 4 tool names match registrations verbatim, HR zone table covers all 5 labels, two worked examples (5x1km threshold + iterative marathon plan), when-NOT-to-call section, safe degradation guidance, and both installation paths documented. No grep-gate violations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md content -> Claude's tool-calling behavior | The skill file is content-only — no code, no compilation. It shapes Claude's prompting behavior. |
| Self-hosted Claude deployments installing this skill | Recipients are assumed to be operators who chose to install polar-flow-mcp; they trust the project to ship truthful guidance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | SKILL.md instructing Claude to leak sensitive info | accept | SKILL.md never asks Claude to share tokens, secrets, or system internals. All examples use placeholder data. No sensitive content. |
| T-03-03-02 | Tampering | SKILL.md tool names drifting from registered names | mitigate | Acceptance criterion `grep -oE 'mcpgo.NewTool\("[^"]+"' internal/mcp/mcp.go` cross-checks every registered tool name appears in SKILL.md verbatim. Drift produces a build-time failure. |
| T-03-03-03 | Spoofing | Malicious upstream modification of SKILL.md tricking Claude into fabricating tool calls | accept | Bundled with the project repository; integrity is the same as any other repo file (git history + signed releases in Phase 4). Out of scope for this phase. |
| T-03-03-04 | Repudiation | Skill telling Claude to silently call tools without user confirmation | mitigate | "Iterative marathon plan" example explicitly instructs ask-then-act behavior; "Listing and deleting" example instructs confirmation before delete. Skill keeps user in the loop for destructive operations. |

Residual risks:
- Polar API endpoint shape is ASSUMED at the polar package layer (Plans 01/02). If the live API rejects a documented request shape, SKILL.md examples may need to update. Mitigation: SKILL.md examples reflect the locked tool interface, not the wire shape — so they remain valid even if the polar package's Polar API JSON construction changes.
</threat_model>

<verification>
- All `<acceptance_criteria>` grep gates satisfied
- `skill/polar-coach/SKILL.md` exists at the documented path
- All 4 registered tool names appear verbatim
- File renders as well-formed markdown (eyeball check during writing)
- No code or test changes; no compilation impact (CI test/lint jobs unaffected)
</verification>

<success_criteria>
- skill/polar-coach/SKILL.md is committed and readable
- Claude can ingest the skill (via local skills directory or project upload) and produce well-formed create_training_target calls from natural-language requests
- HR zone table is unambiguous and matches the handler's label->zone mapping (D-06)
- Skill explicitly defers to the user for plan-scale operations (no silent batch creation)
- Skill gracefully handles the server-unavailable case (degradation guidance present)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-mcp-tools-and-bundled-skill/03-03-SUMMARY.md` listing: file created, line count, all sections present (checklist), and any DEVIATIONS — particularly if any tool name in `internal/mcp/mcp.go` differed from what this plan documented (in which case the actual registered name is authoritative and SKILL.md uses it).
</output>
