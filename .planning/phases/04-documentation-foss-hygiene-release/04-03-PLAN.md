---
phase: 04
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - README.md
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - SECURITY.md
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/PULL_REQUEST_TEMPLATE.md
  - .github/CODEOWNERS
autonomous: true
requirements: [FOSS-01, FOSS-02]
tags: [foss, license, readme, github-templates]

must_haves:
  truths:
    - "`LICENSE` (MIT) is present at repo root with the correct copyright holder"
    - "`README.md` displays 5 working badges (CI, license, latest release, image size, Go report card) all pointing at `lmgarret/polar-flow-mcp`"
    - "`README.md` contains a 5-line docker-compose quickstart and a link to https://lmgarret.github.io/polar-flow-mcp"
    - "`CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` (Contributor Covenant v2.1), `SECURITY.md` are present at repo root"
    - "`.github/ISSUE_TEMPLATE/bug_report.yml` and `feature_request.yml` use YAML issue template format with structured fields"
    - "`.github/PULL_REQUEST_TEMPLATE.md` exists with a PR checklist"
    - "`.github/CODEOWNERS` assigns `* @lmgarret` (minimal default)"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license text with copyright holder"
      contains: "MIT License"
    - path: "README.md"
      provides: "Project front page with badges + quickstart + docs link"
      contains: "img.shields.io"
    - path: "CONTRIBUTING.md"
      provides: "Contributor flow (clone -> make test -> make lint -> PR)"
      contains: "make test"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant v2.1 text"
      contains: "Contributor Covenant"
    - path: "SECURITY.md"
      provides: "Vulnerability reporting process"
      contains: "Reporting"
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Structured bug report form"
      contains: "name: Bug Report"
    - path: ".github/ISSUE_TEMPLATE/feature_request.yml"
      provides: "Structured feature request form"
      contains: "name: Feature Request"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "PR description checklist"
    - path: ".github/CODEOWNERS"
      provides: "Default reviewer assignment"
      contains: "@lmgarret"
  key_links:
    - from: "README.md"
      to: "https://lmgarret.github.io/polar-flow-mcp"
      via: "documentation link in quickstart"
      pattern: "lmgarret.github.io/polar-flow-mcp"
    - from: "README.md badges"
      to: "ci.yml workflow + GHCR image + Go report card"
      via: "shields.io and ghcr-badge URLs"
      pattern: "img.shields.io|ghcr-badge.egpl.dev|goreportcard.com"
    - from: "CONTRIBUTING.md"
      to: "Makefile"
      via: "references make test and make lint targets"
      pattern: "make (test|lint)"
---

<objective>
Author the standard FOSS community files that make the repo publicly releasable: MIT LICENSE, README.md with the 5 required badges and a 5-line quickstart, CONTRIBUTING.md keyed to the existing Makefile, Contributor Covenant v2.1 CODE_OF_CONDUCT.md, SECURITY.md vulnerability reporting policy, structured YAML issue templates, a PR template, and a minimal CODEOWNERS.

Purpose: Satisfy FOSS-01 (license + README + community files) and the non-workflow portion of FOSS-02 (`.github/ISSUE_TEMPLATE/` + `PULL_REQUEST_TEMPLATE.md`). The workflow portion of FOSS-02 (release.yml) is covered in Plan 02.

Output: 9 new files (5 root, 4 under `.github/`). All references to GitHub identity hardcoded to `lmgarret/polar-flow-mcp` per D-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation-foss-hygiene-release/04-CONTEXT.md
@.planning/phases/04-documentation-foss-hygiene-release/04-RESEARCH.md
@.planning/phases/04-documentation-foss-hygiene-release/04-PATTERNS.md
@CLAUDE.md
@Makefile
@docker-compose.yml

<interfaces>
Hardcoded GitHub identity (D-01 — apply everywhere):
- Repo: `lmgarret/polar-flow-mcp`
- GHCR image: `ghcr.io/lmgarret/polar-flow-mcp`
- Pages: `https://lmgarret.github.io/polar-flow-mcp`
- Go Report Card URL: `https://goreportcard.com/report/github.com/lmgarret/polar-flow-mcp` (uses GitHub repo path, NOT Go module path — see Pitfall 5)

Badge URLs (RESEARCH.md Pattern 5 — exact format required):
- CI: `https://img.shields.io/github/actions/workflow/status/lmgarret/polar-flow-mcp/ci.yml?branch=main&label=CI` linking to `https://github.com/lmgarret/polar-flow-mcp/actions/workflows/ci.yml`
- License: `https://img.shields.io/github/license/lmgarret/polar-flow-mcp` linking to `LICENSE`
- Release: `https://img.shields.io/github/v/release/lmgarret/polar-flow-mcp` linking to `https://github.com/lmgarret/polar-flow-mcp/releases/latest`
- Image size: `https://ghcr-badge.egpl.dev/lmgarret/polar-flow-mcp/size` linking to `https://github.com/lmgarret/polar-flow-mcp/pkgs/container/polar-flow-mcp`
- Go Report Card: `https://goreportcard.com/badge/github.com/lmgarret/polar-flow-mcp` linking to `https://goreportcard.com/report/github.com/lmgarret/polar-flow-mcp`

MIT LICENSE boilerplate (RESEARCH.md Code Examples — copyright holder: `LM. Garret`, year: `2026`).

Contributor Covenant v2.1 source: `https://www.contributor-covenant.org/version/2/1/code_of_conduct/` (text must be fetched verbatim — RESEARCH.md Pitfall 6 warns against copying v2.0 boilerplate).

Makefile targets (CONTRIBUTING.md references): `make build`, `make test`, `make lint`, `make clean`.
- `make test` -> `CGO_ENABLED=0 go test -tags=polartest -race -count=1 ./...`
- `make lint` -> `~/go/bin/golangci-lint run ./...`
- golangci-lint version per CLAUDE.md: v2.11

Issue template structure (RESEARCH.md Pattern 6 — YAML format, not Markdown):
- bug_report.yml: name, description, labels, body with markdown intro, version input, auth-proxy dropdown (Authelia/Authentik/oauth2-proxy/Pomerium/Cloudflare Access/Other), what-happened textarea (required), logs textarea with `render: shell`.
- feature_request.yml: name, description, labels, body with markdown intro, summary input (required), use-case textarea (required), proposed-solution textarea (optional).

5-line quickstart (PATTERNS.md README.md section — comment lines count as lines):
- Line 1: `git clone https://github.com/lmgarret/polar-flow-mcp.git`
- Line 2: `cp docker-compose.yml .env.yml  # copy and edit env vars`
- Line 3: `# Set ENCRYPTION_KEY, PROXY_SHARED_SECRET, POLAR_CLIENT_ID, POLAR_CLIENT_SECRET`
- Line 4: `docker compose up -d`
- Line 5: `# Visit https://lmgarret.github.io/polar-flow-mcp for full setup docs`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LICENSE, README.md, and supporting root FOSS files</name>
  <files>LICENSE, README.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md</files>
  <read_first>
    - .planning/phases/04-documentation-foss-hygiene-release/04-RESEARCH.md (MIT boilerplate; Pattern 5 badges; Pitfall 5, 6)
    - .planning/phases/04-documentation-foss-hygiene-release/04-PATTERNS.md (README.md block; CONTRIBUTING.md block)
    - .planning/phases/04-documentation-foss-hygiene-release/04-CONTEXT.md (D-01, specifics — 5-line quickstart)
    - Makefile (target names + commands)
    - docker-compose.yml (env vars referenced in quickstart)
    - CLAUDE.md (Pre-Commit Checklist referenced in CONTRIBUTING.md)
  </read_first>
  <action>
    Write `LICENSE` at repo root using the MIT boilerplate from RESEARCH.md Code Examples verbatim. Copyright line: `Copyright (c) 2026 LM. Garret`.

    Write `README.md` at repo root. Required structure top-to-bottom:
    1. Project title `# polar-flow-mcp` and one-line tagline (`Multi-user MCP server wrapping the Polar AccessLink API for Claude.`).
    2. The 5 badges from RESEARCH.md Pattern 5 in order: CI, License, Latest Release, Image Size, Go Report Card. All using `lmgarret/polar-flow-mcp` per D-01; Go Report Card URL uses `github.com/lmgarret/polar-flow-mcp` (NOT `github.com/lm/polar-flow-mcp` — Pitfall 5).
    3. Placeholder screenshot `![Claude using create_training_target](docs/images/claude-tool-call.png)` (matches the image referenced from docs/index.md and docs/usage.md per DOC-04).
    4. Short "What it does" section (3-5 sentences): create/list/delete Polar training targets from Claude conversations, multi-user via reverse-proxy auth, bundled polar-coach skill.
    5. Section `## Quickstart` containing the 5-line shell snippet from the interfaces block in a fenced bash code block.
    6. Section `## Documentation` linking to `https://lmgarret.github.io/polar-flow-mcp` for full docs (Getting Started, Deployment, Usage, Reference, Security).
    7. Section `## Features` — brief bullet list (4-6 bullets).
    8. Section `## License` — MIT, link to LICENSE file.
    9. Section `## Contributing` — link to CONTRIBUTING.md.

    Write `CONTRIBUTING.md` at repo root. Sections:
    - Welcome paragraph.
    - `## Development setup`: Go 1.26+, install golangci-lint v2.11 to `~/go/bin/golangci-lint` (matches Makefile path).
    - `## Workflow`: fork -> branch -> conventional-commit message (feat/fix/docs/chore/refactor; matches cliff.toml D-10 groups) -> PR.
    - `## Pre-commit checklist`: run `make lint` (resolves to `~/go/bin/golangci-lint run ./...`), run `make test` (resolves to `CGO_ENABLED=0 go test -tags=polartest -race -count=1 ./...`), update docs if behavior changes — mirror the Pre-Commit Checklist section of CLAUDE.md.
    - `## Code of conduct`: link to CODE_OF_CONDUCT.md.
    - `## Reporting security issues`: link to SECURITY.md (do not file public issues for vulnerabilities).

    Write `CODE_OF_CONDUCT.md` at repo root using Contributor Covenant v2.1 verbatim text. Source: `https://www.contributor-covenant.org/version/2/1/code_of_conduct/`. The text contains the title `# Contributor Covenant Code of Conduct`, Our Pledge, Our Standards, Enforcement Responsibilities, Scope, Enforcement, Enforcement Guidelines (with four impact levels: Correction, Warning, Temporary Ban, Permanent Ban), and an Attribution section linking to `https://www.contributor-covenant.org/version/2/1/code_of_conduct.html`. Per Pitfall 6, fetch v2.1 (not v2.0) and replace the placeholder `[INSERT CONTACT METHOD]` with `the project maintainer via the email listed on the GitHub profile of @lmgarret`.

    Write `SECURITY.md` at repo root. Sections:
    - `## Supported versions`: only the latest tagged release is supported (single-developer FOSS project).
    - `## Reporting a vulnerability`: do NOT file a public issue; report privately via GitHub Security Advisories (`https://github.com/lmgarret/polar-flow-mcp/security/advisories/new`) or by emailing the project maintainer (refer to GitHub profile).
    - `## Scope`: vulnerabilities in the polar-flow-mcp server code, deployment defaults that lead to compromise, documentation that leads operators to insecure deployments.
    - `## Out of scope`: vulnerabilities in upstream dependencies (report upstream), social engineering, denial-of-service via load testing.
    - `## Response expectations`: best-effort acknowledgment within 7 days; cross-link to docs/security.md (Plan 01) for the full threat model.

    Every file MUST use `lmgarret/polar-flow-mcp` exclusively for GitHub-facing identity. Do NOT include `github.com/lm/polar-flow-mcp` anywhere (Go module path is internal-only per D-02).
  </action>
  <verify>
    <automated>cd /var/home/lm/git/polar-flow-mcp && ls LICENSE README.md CONTRIBUTING.md CODE_OF_CONDUCT.md SECURITY.md && grep -c 'img.shields.io' README.md && grep -c 'goreportcard.com/badge/github.com/lmgarret/polar-flow-mcp' README.md && grep -c 'lmgarret.github.io/polar-flow-mcp' README.md && grep -c 'Contributor Covenant' CODE_OF_CONDUCT.md && grep -c 'make test' CONTRIBUTING.md && ! grep -l 'github.com/lm/polar-flow-mcp' LICENSE README.md CONTRIBUTING.md CODE_OF_CONDUCT.md SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `LICENSE` exists, line 1 is `MIT License`, contains `Copyright (c) 2026 LM. Garret`
    - File `README.md` exists; title line is `# polar-flow-mcp`
    - `README.md` contains all 5 badge URLs (grep finds at least 1 occurrence of each): `img.shields.io/github/actions/workflow/status/lmgarret/polar-flow-mcp/ci.yml`, `img.shields.io/github/license/lmgarret/polar-flow-mcp`, `img.shields.io/github/v/release/lmgarret/polar-flow-mcp`, `ghcr-badge.egpl.dev/lmgarret/polar-flow-mcp/size`, `goreportcard.com/badge/github.com/lmgarret/polar-flow-mcp`
    - `README.md` contains a fenced bash block with the 5-line quickstart including `docker compose up -d` and `https://lmgarret.github.io/polar-flow-mcp`
    - `README.md` references the placeholder image `docs/images/claude-tool-call.png`
    - File `CONTRIBUTING.md` references both `make test` and `make lint`
    - `CONTRIBUTING.md` mentions conventional commit prefixes `feat`, `fix`, `docs`, `chore`
    - File `CODE_OF_CONDUCT.md` contains `Contributor Covenant` AND `Our Pledge` AND `Enforcement` AND `2.1` (verifying v2.1 not v2.0)
    - `CODE_OF_CONDUCT.md` contains URL `contributor-covenant.org/version/2/1/code_of_conduct.html`
    - `CODE_OF_CONDUCT.md` does NOT contain the literal placeholder `[INSERT CONTACT METHOD]`
    - File `SECURITY.md` contains a `Reporting` heading AND a link to `https://github.com/lmgarret/polar-flow-mcp/security/advisories/new`
    - `SECURITY.md` cross-references `docs/security.md`
    - NONE of the 5 files contains the string `github.com/lm/polar-flow-mcp` (Go module path leak — verified by negated grep)
  </acceptance_criteria>
  <done>5 root FOSS files committed with hardcoded D-01 identity; badge URL formats match RESEARCH.md Pattern 5 exactly so they resolve at runtime once GitHub workflows + releases exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create .github/ ISSUE_TEMPLATE/, PULL_REQUEST_TEMPLATE.md, CODEOWNERS</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/feature_request.yml, .github/PULL_REQUEST_TEMPLATE.md, .github/CODEOWNERS</files>
  <read_first>
    - .planning/phases/04-documentation-foss-hygiene-release/04-RESEARCH.md (Pattern 6 YAML issue template; Open Questions #3 — CODEOWNERS)
    - .planning/phases/04-documentation-foss-hygiene-release/04-PATTERNS.md (bug_report.yml block)
    - .planning/phases/04-documentation-foss-hygiene-release/04-CONTEXT.md (D-01)
  </read_first>
  <action>
    Write `.github/ISSUE_TEMPLATE/bug_report.yml` following RESEARCH.md Pattern 6 and the PATTERNS.md bug_report.yml block. Top-level keys: `name: Bug Report`, `description: File a bug report`, `labels: [bug]`, `body:` array. Body items in order:
    - `type: markdown` with a thank-you intro.
    - `type: input` `id: version`, label `Version`, description "Which Docker image tag or git SHA are you running?", placeholder `ghcr.io/lmgarret/polar-flow-mcp:1.0.0`, `validations: required: true`.
    - `type: dropdown` `id: auth-proxy`, label `Auth proxy`, options exactly `[Authelia, Authentik, oauth2-proxy, Pomerium, "Cloudflare Access", Other]`, `validations: required: true`.
    - `type: textarea` `id: what-happened`, label `What happened?`, description "What did you expect to happen?", `validations: required: true`.
    - `type: textarea` `id: logs`, label `Relevant log output`, `render: shell` (no `required: true`).

    Write `.github/ISSUE_TEMPLATE/feature_request.yml`. Top-level: `name: Feature Request`, `description: Suggest a new tool, configuration option, or improvement`, `labels: [enhancement]`. Body items:
    - `type: markdown` intro.
    - `type: input` `id: summary`, label `Summary`, description one-line summary, `validations: required: true`.
    - `type: textarea` `id: use-case`, label `Use case`, description "What problem does this solve? Who is the user?", `validations: required: true`.
    - `type: textarea` `id: proposed-solution`, label `Proposed solution`, description "If you have an idea for how this should work, describe it here.".
    - `type: textarea` `id: alternatives`, label `Alternatives considered`, optional.

    Write `.github/PULL_REQUEST_TEMPLATE.md` (Markdown, not YAML — GitHub uses Markdown for the PR template). Sections:
    - `## Summary` — one-line description prompt.
    - `## Changes` — bulleted list prompt.
    - `## Testing` — describe how the change was tested.
    - `## Checklist` containing 4 checkboxes, each on its own line: `Tests pass locally (make test)`, `Lint passes (make lint)`, `Documentation updated (if behavior changed)`, `Commit messages follow conventional commits (feat/fix/docs/chore/refactor)`.
    - `## Related issues` — `Closes #` prompt.

    Write `.github/CODEOWNERS` (RESEARCH.md Open Questions #3 — minimal default). Comment header explaining the file assigns @lmgarret as the default reviewer for all paths, followed by a single non-comment line `* @lmgarret`.
  </action>
  <verify>
    <automated>cd /var/home/lm/git/polar-flow-mcp && python3 -c "import yaml; [yaml.safe_load(open(p)) for p in ['.github/ISSUE_TEMPLATE/bug_report.yml','.github/ISSUE_TEMPLATE/feature_request.yml']]" && grep -E 'make test|make lint|conventional' .github/PULL_REQUEST_TEMPLATE.md && grep -E '^\*[[:space:]]+@lmgarret' .github/CODEOWNERS</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/ISSUE_TEMPLATE/bug_report.yml` exists and parses as valid YAML
    - bug_report.yml contains `name: Bug Report`, `labels: [bug]` (or equivalent), and a `body:` array
    - bug_report.yml contains a `type: dropdown` with all 5 proxy options plus `Other`: `Authelia`, `Authentik`, `oauth2-proxy`, `Pomerium`, `Cloudflare Access`, `Other`
    - bug_report.yml `version` input has `placeholder` referencing `ghcr.io/lmgarret/polar-flow-mcp:1.0.0`
    - bug_report.yml `what-happened` and `version` and `auth-proxy` items have `required: true`
    - bug_report.yml logs textarea has `render: shell`
    - File `.github/ISSUE_TEMPLATE/feature_request.yml` exists, parses as valid YAML, name is `Feature Request`, labels include `enhancement`
    - feature_request.yml has at least 3 body items (summary input, use-case textarea, proposed-solution textarea) with summary + use-case marked `required: true`
    - File `.github/PULL_REQUEST_TEMPLATE.md` exists, contains `## Summary`, `## Changes`, `## Testing`, `## Checklist`, `## Related issues` headings
    - PULL_REQUEST_TEMPLATE.md contains all 4 checkbox items naming `make test`, `make lint`, documentation update, and conventional commits
    - File `.github/CODEOWNERS` exists with at least one comment line AND a non-comment line matching the regex `^\*[[:space:]]+@lmgarret`
  </acceptance_criteria>
  <done>4 `.github/` files committed; YAML templates parse; PR template + CODEOWNERS are minimal and correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public reader -> README badges / docs links | Untrusted reader sees badges + quickstart; no code execution path |
| External reporter -> SECURITY.md reporting channel | Vulnerability reports must reach a private channel (GitHub Security Advisories), NOT public issues |
| External contributor -> CONTRIBUTING.md / templates | Templates collect structured input but do not run code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | I | SECURITY.md vulnerability reporting | mitigate | Reporting channel is GitHub Security Advisories (private); public-issue reporting is explicitly discouraged in the doc; acceptance criteria enforces the advisories URL is present |
| T-04-03-02 | T | README.md badges sourced from third-party (shields.io, ghcr-badge.egpl.dev, goreportcard.com) | accept | Badges are read-only image embeds; worst case is a broken image which is cosmetic only — RESEARCH.md A2 documents this risk |
| T-04-03-03 | T | CODE_OF_CONDUCT.md content provenance | mitigate | Fetch v2.1 from the canonical contributor-covenant.org URL (NOT copied from other repos which may contain v2.0 — RESEARCH.md Pitfall 6); acceptance criteria enforces `2.1` token presence |
| T-04-03-04 | I | Go module path leak in user-facing docs | mitigate | Acceptance criteria includes a negated grep for `github.com/lm/polar-flow-mcp` across all 5 root FOSS files; only `lmgarret/polar-flow-mcp` may appear |
| T-04-03-05 | S | README.md image src `docs/images/claude-tool-call.png` | accept | Placeholder image per DOC-04; missing file renders as broken image but no security impact — operator-visible only |
| T-04-03-06 | E | CODEOWNERS misconfiguration | mitigate | Use minimal `* @lmgarret` per RESEARCH.md Open Questions #3; does NOT grant write access (CODEOWNERS only assigns reviewers); acceptance criteria validates exact pattern |
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open(p))"` succeeds for both issue templates
- Negated grep proves no Go module path leak in user-facing files
- All 5 badge URL patterns from RESEARCH.md Pattern 5 are present in README.md
- CODE_OF_CONDUCT.md is v2.1 (not v2.0)
</verification>

<success_criteria>
FOSS-01: LICENSE (MIT), README.md (5 badges + quickstart + docs link), CONTRIBUTING.md, CODE_OF_CONDUCT.md (Contributor Covenant v2.1), SECURITY.md all present at repo root.
FOSS-02 (non-workflow portion): `.github/ISSUE_TEMPLATE/bug_report.yml` + `feature_request.yml` + `.github/PULL_REQUEST_TEMPLATE.md` exist. (The workflow portion — `release.yml` — is delivered by Plan 02.)
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-foss-hygiene-release/04-03-SUMMARY.md`.
</output>
