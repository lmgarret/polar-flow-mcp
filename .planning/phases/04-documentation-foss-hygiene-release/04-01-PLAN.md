---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mkdocs.yml
  - docs/index.md
  - docs/getting-started.md
  - docs/usage.md
  - docs/security.md
  - docs/contributing.md
  - docs/deployment/docker-compose.md
  - docs/deployment/auth-proxies.md
  - docs/deployment/polar-oauth-setup.md
  - docs/reference/env-vars.md
  - docs/reference/mcp-tools.md
  - docs/reference/http-endpoints.md
  - docs/reference/db-schema.md
  - docs/images/.gitkeep
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]
tags: [docs, mkdocs, diataxis]

must_haves:
  truths:
    - "Running `mkdocs build --strict` from repo root produces a site with no warnings or errors"
    - "Every Diátaxis page named in DOC-01 exists under docs/ with fully-written prose (not stubs)"
    - "docs/security.md covers threat model, AES-256-GCM encryption-at-rest scope, fail-closed rationale, header contract, and key rotation plan"
    - "docs/deployment/auth-proxies.md contains real config snippets for Authelia, Authentik, oauth2-proxy, Pomerium, and Cloudflare Access"
    - "Placeholder image references use the syntax `![placeholder](images/<name>.png)` at logical screenshot points"
    - "All GitHub URLs in mkdocs.yml and docs cross-links use `lmgarret/polar-flow-mcp` per D-01"
  artifacts:
    - path: "mkdocs.yml"
      provides: "MkDocs Material site config (site_name, site_url, repo_url, theme, nav)"
      contains: "site_url: https://lmgarret.github.io/polar-flow-mcp"
    - path: "docs/index.md"
      provides: "Project overview landing page"
    - path: "docs/getting-started.md"
      provides: "Prereqs → link Polar → first tool call"
    - path: "docs/usage.md"
      provides: "MCP tool walkthroughs + skill installation"
    - path: "docs/security.md"
      provides: "Threat model + crypto scope + header contract + rotation plan"
    - path: "docs/contributing.md"
      provides: "Contributor flow (clone → make test → make lint → PR)"
    - path: "docs/deployment/docker-compose.md"
      provides: "Full docker-compose.yml walkthrough"
    - path: "docs/deployment/auth-proxies.md"
      provides: "Header contract config snippets for 5 proxy types"
    - path: "docs/deployment/polar-oauth-setup.md"
      provides: "Polar developer app registration walkthrough"
    - path: "docs/reference/env-vars.md"
      provides: "All env vars with defaults + examples"
    - path: "docs/reference/mcp-tools.md"
      provides: "MCP tool schemas (input/output)"
    - path: "docs/reference/http-endpoints.md"
      provides: "All HTTP routes documented"
    - path: "docs/reference/db-schema.md"
      provides: "SQLite schema reference"
    - path: "docs/images/.gitkeep"
      provides: "Empty directory for placeholder images"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/*.md"
      via: "nav: entries map to existing files"
      pattern: "nav:"
    - from: "docs/index.md"
      to: "docs/getting-started.md"
      via: "internal link"
      pattern: "getting-started.md"
---

<objective>
Create the complete MkDocs Material documentation site (Diátaxis structure with fully written prose) covering every deployment scenario, the security model, all MCP tools, and all reference material. The output is the static content that `mkdocs gh-deploy` (Plan 02) will publish to GitHub Pages.

Purpose: Make the project deployable by a self-hoster from documentation alone (D-06). Cover requirements DOC-01..04.

Output: `mkdocs.yml` at repo root + `docs/` tree with 13 prose pages + empty `docs/images/` directory. `mkdocs build --strict` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation-foss-hygiene-release/04-CONTEXT.md
@.planning/phases/04-documentation-foss-hygiene-release/04-RESEARCH.md
@.planning/phases/04-documentation-foss-hygiene-release/04-PATTERNS.md
@CLAUDE.md
@docker-compose.yml
@Makefile
@skill/polar-coach/SKILL.md

<interfaces>
GitHub identity (hardcode everywhere, per D-01):
- GitHub repo: `lmgarret/polar-flow-mcp`
- GHCR image: `ghcr.io/lmgarret/polar-flow-mcp`
- Pages URL: `https://lmgarret.github.io/polar-flow-mcp`
- Go module (internal only, NOT in user-facing docs): `github.com/lm/polar-flow-mcp`

MkDocs Material nav structure (mkdocs.yml `nav:`):
- Home → index.md
- Getting Started → getting-started.md
- Deployment → deployment/{docker-compose,auth-proxies,polar-oauth-setup}.md
- Usage → usage.md
- Reference → reference/{env-vars,mcp-tools,http-endpoints,db-schema}.md
- Security → security.md
- Contributing → contributing.md

Env vars (from docker-compose.yml, authoritative):
- REQUIRED: AUTH_PROXY, PROXY_SHARED_SECRET, ENCRYPTION_KEY, POLAR_CLIENT_ID, POLAR_CLIENT_SECRET
- OPTIONAL: IDENTITY_HEADER (default Remote-User), BIND_ADDRESS (default 127.0.0.1), DATABASE_PATH (default polar.db)

HTTP endpoints (from internal/ — verify exact paths during implementation):
- GET /healthz (no auth, always 200)
- GET /readyz (no auth, 200 if AUTH_PROXY configured + secret set + DB reachable + key loaded; else 503)
- GET /oauth/login (proxy-auth required; redirects to flow.polar.com)
- GET /oauth/callback (proxy-auth required; consumes state, exchanges code, stores encrypted token)
- POST /mcp and /mcp/ (proxy-auth required; StreamableHTTP MCP transport)

MCP tools (from skill/polar-coach/SKILL.md, authoritative):
- get_user_info
- create_training_target
- list_training_targets
- delete_training_target

DB schema (from internal/store/migrations/ — read during implementation):
- users (identity, polar_user_id, timestamps)
- polar_tokens (encrypted_token BLOB nonce||ciphertext, key_version, updated_at)
- pending_auth (CSRF state, expiry)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mkdocs.yml and docs/ skeleton with stable Tier-1 content</name>
  <files>mkdocs.yml, docs/index.md, docs/getting-started.md, docs/contributing.md, docs/images/.gitkeep, docs/reference/env-vars.md, docs/reference/http-endpoints.md, docs/reference/db-schema.md</files>
  <read_first>
    - .planning/phases/04-documentation-foss-hygiene-release/04-RESEARCH.md (Pattern 3 mkdocs.yml; Architectural Responsibility Map)
    - .planning/phases/04-documentation-foss-hygiene-release/04-PATTERNS.md (mkdocs.yml block; env-vars analog)
    - .planning/phases/04-documentation-foss-hygiene-release/04-CONTEXT.md (D-01, D-06, D-07, D-08)
    - docker-compose.yml (env var authoritative list)
    - Makefile (test/lint targets for contributing.md)
    - cmd/polar-flow-mcp/main.go (route registrations for http-endpoints.md)
    - internal/store/migrations/ (read all *.sql files for db-schema.md)
    - CLAUDE.md (tech stack section for index.md)
  </read_first>
  <action>
    Create `mkdocs.yml` at repo root using the Pattern 3 config from RESEARCH.md verbatim: site_name `polar-flow-mcp`, site_url `https://lmgarret.github.io/polar-flow-mcp`, repo_url `https://github.com/lmgarret/polar-flow-mcp`, repo_name `lmgarret/polar-flow-mcp`, theme `material` with palette primary `deep orange` + accent `orange`, features `navigation.tabs`, `navigation.sections`, `navigation.expand`, `toc.integrate`, `search.suggest`, plugins `search` only (no `social`, no `mike` per D-07), and the full nav block exactly as in RESEARCH.md Pattern 3.

    Create `docs/images/.gitkeep` (empty file) to commit the directory.

    Author `docs/index.md` (Diátaxis Home, ~80-120 lines): project tagline, what it is (multi-user MCP server wrapping Polar AccessLink), core value proposition ("create a 5x1km threshold session for Thursday" → Polar Flow), key features bullet list, tech stack summary (Go 1.26, modernc.org/sqlite, AES-256-GCM, StreamableHTTP MCP), reverse-proxy auth model summary (one sentence + link to security.md), placeholder image `![Claude using create_training_target](images/claude-tool-call.png)`, and cross-links to Getting Started + Deployment + Security.

    Author `docs/getting-started.md` (Diátaxis Tutorial, ~150-200 lines): prerequisites (existing Polar account, reverse-proxy already deployed: Authelia/Authentik/oauth2-proxy/Pomerium/Cloudflare Access, Docker), step-by-step path: (1) register Polar developer app → link to deployment/polar-oauth-setup.md, (2) generate `ENCRYPTION_KEY` via `openssl rand -base64 32` and `PROXY_SHARED_SECRET` via `openssl rand -hex 32`, (3) copy docker-compose.yml and fill required env vars from docker-compose.yml comments, (4) configure reverse proxy to inject `Remote-User` and `X-Proxy-Secret` headers → link to deployment/auth-proxies.md, (5) `docker compose up -d`, (6) visit `/oauth/login` to link Polar account, (7) install polar-coach skill in Claude (link to usage.md), (8) verify with "what Polar account is linked?" → `get_user_info`. Include placeholder image `![Polar OAuth login page](images/polar-oauth-flow.png)` between steps 5 and 6.

    Author `docs/contributing.md` (Diátaxis How-To, ~80-120 lines): clone repo, install Go 1.26+ and golangci-lint v2.11 to `~/go/bin/golangci-lint`, build with `make build`, test with `make test` (which runs `CGO_ENABLED=0 go test -tags=polartest -race -count=1 ./...`), lint with `make lint` (runs `~/go/bin/golangci-lint run ./...`), conventional-commit message format (feat/fix/docs/chore/refactor), PR checklist (tests pass, lint clean, docs updated), link to CONTRIBUTING.md in repo root as the authoritative version.

    Author `docs/reference/env-vars.md` (Diátaxis Reference, ~150-200 lines): table format with columns `Variable | Required | Default | Description | How to set`. Cover all 8 vars from docker-compose.yml: AUTH_PROXY (REQUIRED, no default — server refuses to start if `unconfigured`), PROXY_SHARED_SECRET (REQUIRED, generate with `openssl rand -hex 32`), ENCRYPTION_KEY (REQUIRED 32-byte base64, generate with `openssl rand -base64 32`), KEY_PROVIDER (OPTIONAL, default `env`, alternatives `file`), ENCRYPTION_KEY_FILE (REQUIRED if KEY_PROVIDER=file), POLAR_CLIENT_ID, POLAR_CLIENT_SECRET (REQUIRED, both from polaraccesslink.com), IDENTITY_HEADER (default `Remote-User`), BIND_ADDRESS (default `127.0.0.1` — non-loopback emits WARN log per SERV-05), DATABASE_PATH (default `polar.db`). Each row links to security.md for the rationale where applicable.

    Author `docs/reference/http-endpoints.md` (Diátaxis Reference, ~80-120 lines): for each route, columns `Method | Path | Auth | Purpose | Returns`. Routes derived from cmd/polar-flow-mcp/main.go: GET /healthz (no auth, 200), GET /readyz (no auth, 200/503 with named failing checks), GET /oauth/login (proxy auth required, 302 to flow.polar.com), GET /oauth/callback (proxy auth required, exchanges code, stores token), POST /mcp + /mcp/ (proxy auth required, StreamableHTTP MCP transport). Note that all non-health routes require both identity header and `X-Proxy-Secret` header via `subtle.ConstantTimeCompare`.

    Author `docs/reference/db-schema.md` (Diátaxis Reference, ~80-120 lines): describe each table from internal/store/migrations/*.sql. Columns and types for `users` (identity TEXT UNIQUE NOT NULL, polar_user_id TEXT, timestamps), `polar_tokens` (user_id FK, encrypted_token BLOB containing 12-byte nonce || ciphertext, key_version INTEGER, updated_at), `pending_auth` (state TEXT, identity TEXT, expires_at TIMESTAMP). Note WAL mode + dual-pool design (FOUND-05) and AES-256-GCM crypto schema (FOUND-08).

    Every page MUST use hardcoded `lmgarret/polar-flow-mcp` for any GitHub URL. No Go module path `github.com/lm/polar-flow-mcp` may appear in user-facing prose.
  </action>
  <verify>
    <automated>cd /var/home/lm/git/polar-flow-mcp && pip install --quiet mkdocs-material 2>&amp;1 | tail -5 && mkdocs build --strict 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `mkdocs.yml` exists at repo root and contains `site_url: https://lmgarret.github.io/polar-flow-mcp`
    - `mkdocs.yml` contains `theme:` block with `name: material` (grep `mkdocs.yml | grep -c 'name: material'` returns ≥1)
    - `mkdocs.yml` contains nav entries for all 13 pages listed in files_modified
    - `mkdocs.yml` does NOT contain `mike` or `social` (grep returns 0)
    - File `docs/index.md` contains `# ` heading at line 1 and at least one cross-link to `getting-started.md`
    - File `docs/getting-started.md` contains the string `openssl rand -base64 32` and `openssl rand -hex 32` and `docker compose up`
    - File `docs/getting-started.md` contains `![` placeholder image syntax referencing `images/`
    - File `docs/contributing.md` references both `make test` and `make lint`
    - File `docs/reference/env-vars.md` contains all 5 REQUIRED env var names: `AUTH_PROXY`, `PROXY_SHARED_SECRET`, `ENCRYPTION_KEY`, `POLAR_CLIENT_ID`, `POLAR_CLIENT_SECRET`
    - File `docs/reference/http-endpoints.md` contains paths `/healthz`, `/readyz`, `/oauth/login`, `/oauth/callback`, `/mcp`
    - File `docs/reference/db-schema.md` names all 3 tables: `users`, `polar_tokens`, `pending_auth`
    - File `docs/images/.gitkeep` exists (zero bytes acceptable)
    - No file under `docs/` contains the string `github.com/lm/polar-flow-mcp` (grep returns 0 — only `lmgarret/polar-flow-mcp` permitted)
    - Command `mkdocs build --strict` exits 0 from repo root
  </acceptance_criteria>
  <done>mkdocs.yml committed; 7 Tier-1 docs pages authored as full prose; mkdocs build --strict succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Author deployment/, usage, security pages (the high-prose pages)</name>
  <files>docs/usage.md, docs/security.md, docs/deployment/docker-compose.md, docs/deployment/auth-proxies.md, docs/deployment/polar-oauth-setup.md, docs/reference/mcp-tools.md</files>
  <read_first>
    - .planning/phases/04-documentation-foss-hygiene-release/04-RESEARCH.md (Security Domain; user_constraints D-06)
    - .planning/phases/04-documentation-foss-hygiene-release/04-CONTEXT.md (specifics — security.md content list; auth proxy list)
    - .planning/phases/04-documentation-foss-hygiene-release/04-PATTERNS.md (usage.md/mcp-tools.md analog)
    - skill/polar-coach/SKILL.md (authoritative tool list + zone table + worked examples)
    - docker-compose.yml (env vars + fail-closed comments)
    - CLAUDE.md (critical decisions table — middleware ordering, crypto schema)
    - .planning/STATE.md (Key Decisions Locked — middleware ordering, encryption layout)
    - mkdocs.yml (just created — verify nav matches files)
  </read_first>
  <action>
    Author `docs/usage.md` (Diátaxis Tutorial+How-To, ~200-250 lines): summarize all four MCP tools (`get_user_info`, `create_training_target`, `list_training_targets`, `delete_training_target`) in a usage-oriented voice. Include the same worked examples as SKILL.md (5x1km threshold; iterative marathon plan), the HR zone label table (easy→Z1-2, aerobic→Z2, tempo→Z3, threshold→Z4, vo2max→Z5), and both skill installation paths from SKILL-04: (1) Claude Desktop/Code via `~/.claude/skills/polar-coach/SKILL.md` symlink or copy, (2) Claude.ai project upload of the same file. Add placeholder image `![Claude tool call](images/claude-tool-call.png)`. Cross-link to reference/mcp-tools.md for the full schemas.

    Author `docs/security.md` (Diátaxis Explanation, ~250-350 lines) — D-06 mandates full prose. Sections (all REQUIRED per DOC-02):
    - **Threat model**: trust boundaries (client → reverse proxy → polar-flow-mcp → Polar API), what each boundary enforces, who the adversaries are (network attacker, malicious local user with proxy bypass attempt, compromised Polar API).
    - **Encryption at rest scope**: AES-256-GCM with 12-byte nonce || ciphertext stored as single BLOB; what this protects (DB file copy without key); what this does NOT protect (memory dump while server is running, compromised key file); key_version column present but rotation deferred to v2 (OPS-02).
    - **Fail-closed rationale**: server refuses to start unless `AUTH_PROXY ≠ "unconfigured"`, `PROXY_SHARED_SECRET` set, encryption key loadable, DB reachable; `docker-compose.yml` ships with `AUTH_PROXY=unconfigured` as a UX contract for operators (SERV-08); rationale: silent default to insecure mode is the most common self-host footgun.
    - **Header contract**: identity header (default `Remote-User`, configurable via `IDENTITY_HEADER`) injected by trusted reverse proxy; `X-Proxy-Secret` header verified via `subtle.ConstantTimeCompare` BEFORE identity header is read; this ordering is non-negotiable (prevents timing oracle on the secret).
    - **Key rotation plan**: v1 has no rotation tooling; `key_version` column ready; v2 plan is to support decrypting key_version=1 + re-encrypting with key_version=2; until then, key rotation requires re-running the OAuth flow per user.
    Add a final "Reporting vulnerabilities" section linking to `SECURITY.md` in repo root.

    Author `docs/deployment/docker-compose.md` (Diátaxis How-To, ~150-200 lines): walkthrough of every line of `docker-compose.yml`. Explain `image: ghcr.io/lmgarret/polar-flow-mcp:latest` (note that the file shipped in the repo currently uses `ghcr.io/lm/polar-flow-mcp:latest` — operators should update to the public image after Phase 4 release); explain the named volume `polar-data:/data`; explain each env var with the same REQUIRED/OPTIONAL labels from the file. Show a complete filled example with `<replace-me>` placeholders. Add placeholder image `![docker compose up output](images/docker-compose-up.png)`.

    Author `docs/deployment/auth-proxies.md` (Diátaxis How-To, ~400-500 lines) — D-03 mandates real config snippets for all 5 proxies. For EACH of Authelia, Authentik, oauth2-proxy, Pomerium, Cloudflare Access:
    - one-paragraph intro on how that proxy works
    - the exact header config required: must inject `Remote-User` (or whatever `IDENTITY_HEADER` is set to) AND `X-Proxy-Secret` matching `PROXY_SHARED_SECRET`
    - a real config snippet (YAML, JSON, or product-specific syntax) showing the polar-flow-mcp protected route
    - note that any proxy that injects both headers works
    Use code-fenced blocks with the proxy's native config language. Add placeholder image `![Authelia login screen](images/authelia-login.png)`.

    Author `docs/deployment/polar-oauth-setup.md` (Diátaxis How-To, ~100-150 lines): Polar developer console walkthrough — visit `https://admin.polaraccesslink.com`, create app, set redirect URI to `https://<your-host>/oauth/callback`, request `accesslink.read_all` scope, copy client_id and client_secret to `POLAR_CLIENT_ID` and `POLAR_CLIENT_SECRET` env vars. Add placeholder image `![Polar developer console app registration](images/polar-app-registration.png)`.

    Author `docs/reference/mcp-tools.md` (Diátaxis Reference, ~200-300 lines): formal schema reference for all 4 tools — tool name, JSON input schema (parameter names, types, required/optional, defaults), JSON output shape, error conditions. For `create_training_target` include the full phases array shape (type warmup/repeat/cooldown, repeat count, duration/distance, intensity by hr_zone integer 1-5 or intensity_label one of easy/aerobic/tempo/threshold/vo2max, optional recovery sub-phase). Source: actual handler signatures + SKILL.md tool definitions.
  </action>
  <verify>
    <automated>cd /var/home/lm/git/polar-flow-mcp &amp;&amp; mkdocs build --strict 2>&amp;1 | tail -30 &amp;&amp; for p in Authelia Authentik oauth2-proxy Pomerium "Cloudflare"; do echo -n "$p: "; grep -c "$p" docs/deployment/auth-proxies.md; done</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/usage.md` references all 4 MCP tool names: `get_user_info`, `create_training_target`, `list_training_targets`, `delete_training_target`
    - File `docs/usage.md` contains both `easy` and `threshold` (HR zone labels)
    - File `docs/usage.md` documents both installation paths (mentions `~/.claude/skills` and `claude.ai`)
    - File `docs/security.md` contains all five required topic markers (one heading per topic): `Threat model`, `Encryption`, `Fail-closed`, `Header contract`, `Key rotation` (case-insensitive heading match acceptable)
    - File `docs/security.md` contains `AES-256-GCM` and `subtle.ConstantTimeCompare`
    - File `docs/deployment/auth-proxies.md` contains all 5 proxy names: `Authelia`, `Authentik`, `oauth2-proxy`, `Pomerium`, `Cloudflare Access` (each appearing ≥3 times — intro + config + summary)
    - File `docs/deployment/auth-proxies.md` contains at least 5 code-fenced blocks (grep `^\`\`\`` returns ≥10 lines for open+close pairs)
    - File `docs/deployment/docker-compose.md` references the corrected image path `ghcr.io/lmgarret/polar-flow-mcp`
    - File `docs/deployment/polar-oauth-setup.md` references `https://admin.polaraccesslink.com` and `/oauth/callback`
    - File `docs/reference/mcp-tools.md` documents the phases array shape and intensity_label values (`easy`, `aerobic`, `tempo`, `threshold`, `vo2max`)
    - Each authored doc page contains at least one `![placeholder](images/...png)` reference per DOC-04
    - Command `mkdocs build --strict` exits 0 with zero warnings in stderr
    - No user-facing doc page contains `github.com/lm/polar-flow-mcp` (Go module path leak)
  </acceptance_criteria>
  <done>All 6 high-prose pages authored as complete documentation (not stubs); `mkdocs build --strict` passes with the full nav.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader → docs site | Untrusted reader consumes published static HTML — no input crosses into the running server |
| Author → git → GitHub Pages | Documentation source flows through git into the gh-pages branch via CI; covered by Plan 02's CI supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | I | docs/security.md prose | mitigate | Documenting the threat model is the goal; prose must not leak internal Go module path `github.com/lm/polar-flow-mcp` or operator secrets — acceptance criteria enforces zero occurrences in docs/ |
| T-04-01-02 | T | docs/deployment/auth-proxies.md config snippets | mitigate | Snippets must specify both `Remote-User` AND `X-Proxy-Secret` requirements; misleading half-configs (identity header only, no shared secret) would let operators deploy insecure setups — reviewers verify every snippet injects both headers |
| T-04-01-03 | I | docs/reference/db-schema.md | accept | Schema is open by design (FOUND-07); knowing the table layout does not enable attack since attackers cannot reach the DB through documented endpoints |
| T-04-01-04 | S | docs/index.md / branding | accept | Site identity (site_url, repo_url) hardcoded to D-01 values; impersonation risk is GitHub-side, not docs-side |
</threat_model>

<verification>
- `mkdocs build --strict` exits 0
- All 13 nav entries resolve to real files
- No occurrences of `github.com/lm/polar-flow-mcp` in user-facing docs (only `lmgarret/polar-flow-mcp`)
- All 5 auth proxies have real config snippets, not placeholders
- DOC-02 security.md covers all 5 mandated topics
</verification>

<success_criteria>
DOC-01: `docs/` tree matches Diátaxis structure exactly as specified in REQUIREMENTS.md.
DOC-02: `docs/security.md` covers threat model, encryption-at-rest scope, fail-closed rationale, header contract, and key rotation plan.
DOC-03: `docs/deployment/auth-proxies.md` covers Authelia, Authentik, oauth2-proxy, Pomerium, Cloudflare Access with real config snippets.
DOC-04: Placeholder images use `![placeholder](images/<descriptive-name>.png)` syntax at logical points.
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-foss-hygiene-release/04-01-SUMMARY.md`.
</output>
